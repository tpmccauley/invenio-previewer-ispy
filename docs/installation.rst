============
Installation
============

At the command line::

    $ easy_install invenio-previewer-ispy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv invenio-previewer-ispy
    $ pip install invenio-previewer-ispy